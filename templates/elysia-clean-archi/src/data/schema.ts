// MongoDB schema definition for Drizzle is often schema-less or defined via types
// But we can define collections if using specific driver patterns or validation
// For this template, we will just export empty object or basic types as placeholder
export const schema = {};
